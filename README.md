# Microtonic-BeatSpace-Multitouch-Bridge
Standalone OSC bridge for BeatSpace (Sonic Charge Microtonic).   Receives OSC multitouch messages and writes them to a JSON file that the BeatSpace Microtonic script reads in real time.
